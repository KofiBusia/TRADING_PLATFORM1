services:
  - type: web
    name: trading-platform1
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 4 --timeout 60 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.1
